<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="VVMusic.Views.MusicListPage">
    <ContentPage.Content>
        <StackLayout>
            <CollectionView ItemsSource="{Binding MusicListItemViewModels}" SelectionMode="Single" SelectedItem="{Binding CurrentSelectItem}"
                            SelectionChangedCommand="{Binding SelectChangedCommand}" SelectionChangedCommandParameter="{Binding}">
                <CollectionView.ItemTemplate>
                    <DataTemplate>
                        <Label Text="{Binding Name}"></Label>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>